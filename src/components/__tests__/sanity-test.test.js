import { test } from 'vitest';

test('sanity test', () => {
  return;
});

// if you return true or false , it still "passes" - so, we'd better find a better way to talk about this

// note coverate-v8 doesn't work on StackBlitz
